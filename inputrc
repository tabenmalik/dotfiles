# Respect default shortcuts
$include /etc/inputrc

# I despise the terminal bell
set bell-style none

# Arrow up performs partial completion search of history
"\e[A":history-search-backward

# Arrow down performs partial completion search of history
"\e[B":history-search-forward
